# RECOMMENDATION SERVICE — BLUE (v1)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service-blue
  namespace: streamflix
  labels:
    app: recommendation-service
    version: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation-service
      version: blue
  template:
    metadata:
      labels:
        app: recommendation-service
        version: blue
    spec:
      initContainers:
        - name: wait-for-mongo-and-redis
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "waiting for mongo:27017";
              until nc -z mongo 27017; do echo "mongo not ready"; sleep 2; done;
              echo "mongo reachable";
              echo "waiting for redis:6379";
              until nc -z redis 6379; do echo "redis not ready"; sleep 2; done;
              echo "redis reachable";
      containers:
        - name: recommendation-service
          image: ${REGISTRY}/recommendation-service:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: MONGO_URI
              value: mongodb://mongo:27017
            - name: REDIS_HOST
              value: redis
            - name: MONGO_DB
              value: streamflix_movies
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 6
            timeoutSeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            timeoutSeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "150m"
              memory: "256Mi"

---
# RECOMMENDATION SERVICE — GREEN (v2)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service-green
  namespace: streamflix
  labels:
    app: recommendation-service
    version: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation-service
      version: green
  template:
    metadata:
      labels:
        app: recommendation-service
        version: green
    spec:
      initContainers:
        - name: wait-for-mongo-and-redis
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "waiting for mongo:27017";
              until nc -z mongo 27017; do echo "mongo not ready"; sleep 2; done;
              echo "mongo reachable";
              echo "waiting for redis:6379";
              until nc -z redis 6379; do echo "redis not ready"; sleep 2; done;
              echo "redis reachable";
      containers:
        - name: recommendation-service
          image: ${REGISTRY}/recommendation-service:v2
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: MONGO_URI
              value: mongodb://mongo:27017
            - name: REDIS_HOST
              value: redis
            - name: MONGO_DB
              value: streamflix_movies
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 6
            timeoutSeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            timeoutSeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "150m"
              memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: streamflix
spec:
  selector:
    app: recommendation-service
    version: blue
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
